@layer base {
  .wrapper {
    min-height: 100vh;
    @apply my-0 pt-20 mx-auto flex justify-center items-start flex-col;
  }

  main {
    @apply flex flex-col flex-auto w-full mx-auto pt-5 pb-16;
  }
}
